# PC Controller App Dependencies (Cross-Platform: Windows, macOS, Linux)

# GUI Framework (PySide6 is the official Qt binding for Python)
PySide6>=6.6.1

# Networking
python-socketio[asyncio]>=5.10.0  # Use asyncio extra for modern async apps
websockets>=11.0.3
zeroconf>=0.69.0
pure-python-adb>=0.3.0.dev0

# Video Processing & Data Handling
opencv-python>=4.8.0.74  # Use the standard version for GUI apps
numpy>=1.24.3
pillow>=10.0.0
pandas>=2.0.3
matplotlib>=3.7.2
pyqtgraph>=0.13.7

# Sensor Integration & Experiment Control
pyserial>=3.5
bleak==0.12.1  # CORRECT: Pinned to the last version compatible with pyobjc 7.x
pylsl>=1.16.2
pyshimmer==0.7.0  # Pin to the latest known working version

# macOS specific dependency for psychopy and bleak.
# psychopy requires a version < 8, so we pin to the latest 7.x release (7.3).
# This version is also compatible with bleak==0.12.1.
pyobjc==7.3

# PsychoPy - A large scientific software package for experiment control
psychopy>=2023.2.3

# Utilities
pyyaml>=6.0.1
python-dateutil>=2.8.2
